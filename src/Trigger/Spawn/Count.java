package Trigger.Spawn;

import Main.GameMap;

import java.util.Set;

public class Count extends SpawnTriggers{
    public Count(Set<Integer> set, int Spawn, GameMap gameMap) {
        super(set, Spawn, gameMap);
    }
}
